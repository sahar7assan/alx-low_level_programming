#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char mystring[] = "\"Programming is like building a multilingual puzzle";

	puts(mystring);
	return (0);
}
