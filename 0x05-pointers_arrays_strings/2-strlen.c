#include "main.h"
/**
 * _strlen -  function that returns the length of a string
 * @s: input value
 * Return: void (Succsess)
 */
int _strlen(char *s)
{

	int i;
	int count;

	for (i = 0 ; s[i] != '\0' ; i++)
	{
		count = i;
	}

	return (count);
}
