#include "main.h"
/**
 * print_rev -  prints a string, in reverse
 * @s: input value
 * Return: void (Succsess)
 */
void print_rev(char *s)
{

	int i , j , len;

	for (i = 0 ; s[i] != '\0' ; i++)
	{
		len = i;
	}
	for (j = len ; j <= len ; j--)
	{
		_putchar(s[i]);
	}
	_putchar('\n');
}
